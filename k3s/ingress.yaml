apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: app-fe-ingress
  namespace: xora-cicd
  annotations:
    kubernetes.io/ingress.class: traefik
spec:
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: app-fe-service
                port:
                  number: 80
